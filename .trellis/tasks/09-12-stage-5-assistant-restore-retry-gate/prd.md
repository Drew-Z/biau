# 公开助手恢复重试与历史操作互斥

## 目标与证据

恢复失败后，访客可以打开历史会话、关闭抽屉，再点击仍可用的“重试恢复”。当前 `retryInitialRestore` 没有遵守历史操作门禁，允许两个恢复流程同时决定会话是否可用。若 A 的重试先成功，B 的恢复随后失败，页面会保留 A 的历史却重新禁用输入框。

2026-09-12 主会话在未修改的 `e72ed5428868046e9eb299d9af73c3578fa772cf` 源码/现有构建上，以本地 fixture 在 1440/Morning/中文和 320/Stellar/英文复现 2/2；A 的输入框从可用再次变为禁用，草稿/身份仍属于 A，无聊天请求、无外部请求或页面错误。证据位于 `C:/Users/zhang/AppData/Local/Temp/blog-semi-assistant-restore-retry-1icsdx4s/assessment-before.json`，探针真实 exit 1。

## 范围

- 初次恢复错误提示的重试按钮与 `retryInitialRestore` 命令共享现有 history pending 门禁。
- 历史恢复和删除进行中，关闭抽屉后仍不能额外发起当前会话恢复；同一事件批次也受同步 ref 保护。
- 成功、失败、过期后按既有规则释放门禁。未恢复的当前会话仍须显式重试或新建；不自动发送问题或恢复其他 capability。
- 只读历史列表刷新继续允许当前会话恢复重试；离线和 Retry-After 保持原规则。
- 扩展已有历史浏览器矩阵并记录旧构建失败、最终构建通过及相同受检输入。

## 验收标准

- [x] 历史恢复/删除 pending 时重试按钮禁用，命令不新增 POST，即使在历史动作与重试的同一事件批次中。
- [x] 当前/其他会话的恢复成功、503、过期，以及删除成功/503，均保留正确身份、草稿和显式恢复路径。
- [x] 操作结束不自动重试或发问；只读列表 pending 不阻止显式恢复；新建命令仍可用。
- [x] 历史矩阵由 100 扩展到 152 场景，覆盖四个既有宽度/主题/语言配置，真实模型调用为 0。
- [x] lint/build、助手 API/会话/browser-state 合同、Branch 32、image 48、性能、smoke 和完整 UI 通过；检查绑定最终源码/构建。
- [ ] 仅本地精确范围提交、归档本子任务并实际返回父任务；保留原 13 份未跟踪资料和保护快照。

## 授权与排除项

父任务 `loop.md` 的持续授权及本次“继续”覆盖规划、启动、本地修改/验证/提交。保持不 push/deploy/sign、不调用生产模型/DB/relay、不公开内容、不启用 Feed/Cron、不修改未知 heartbeat；authored/SEO 翻译继续暂缓。不修改 `public/status/blog-semi-synthetic.json`，不重做已交付子任务，不改变 API、文案、样式或依赖。
