# Implement: Mobile Assistant Chat-First Workspace

1. Add mobile drawer state, focus management, Escape handling, and scroll locking to `AssistantPage`.
2. Add the mobile controls trigger, backdrop, dialog semantics, and close control while reusing the existing sidebar body.
3. Add chat-first mobile ordering and safe bounded drawer styles at the existing 920px breakpoint.
4. Extend UI checks for desktop inverse visibility and 320/390/430px ordering, modal interaction, focus, scroll lock, bounds, and overflow.
5. Capture before/after mobile evidence, update frontend specs, and run the full quality gate.