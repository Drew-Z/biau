# 公开助手恢复失败提示与操作状态一致

## Goal

访客打开保存过的匿名会话、恢复请求失败时，界面准确说明当前会话需要恢复，并指向可用的恢复或新建动作。不声称当前可以直接提问、已经发起研究或已经获得兜底回答。

## Evidence

- 基线为 `1d2b72d8ee96ffef83877be266021f3788896349`。源文件 506、构建文件 172、相关规范 3 与上一轮最终冻结结果一致。
- 本地 fixture 在 1440/Morning/中文、320/Stellar/英文覆盖服务不可用、数据库未配置、未知失败、超时，8/8 提示与恢复状态不符。
- 服务错误显示“当前仍可提问”，但输入和发送均禁用；未知错误显示“已保留站内兜底结果”，实际消息数为 0；超时被称为研究超时，实际只请求过 health 和 session。
- 草稿保留、模型调用 0、聊天请求 0、外部请求 0、页面错误 0。证据：`C:/Users/zhang/AppData/Local/Temp/blog-semi-assistant-restore-copy-5p83n7xv/assessment-before.json` 及同目录截图。

## Requirements

- R1：普通恢复失败使用现有中英文恢复提示，明确重试恢复或新建空白会话。
- R2：离线/恢复联网、限流及 Retry-After、已经收到回答但权威历史刷新失败仍使用各自专用提示。
- R3：保持现有草稿、会话身份、恢复门禁和请求生命周期；提示变化不触发自动请求或发送。
- R4：已恢复会话的历史列表错误、聊天、分支和预热错误保持既有表现。
- R5：桌面和 320/390/430、三个主题及中英文的提示/按钮可读可用，显式恢复、新建和后续发送按当前身份工作。

## Acceptance Criteria

- [x] 8 个负向场景在最终构建上不再出现误导提示，且有永久浏览器回归。
- [x] 错误分类、离线恢复、限流倒计时、语言切换、显式恢复/新建均保留身份和请求边界。
- [x] 原历史矩阵保留，正常历史列表错误及已有回答刷新提示通过对照检查。
- [x] lint、build、相关合同、浏览器专项、性能、smoke 和完整 UI 取得真实终局。
- [ ] 按精确文件范围本地提交，仅归档本子任务并实际返回父路线图。

## Scope And Constraints

- 修改只涉及组件的错误文案投影、现有历史 UI 检查、必要规范与任务记录。
- 复用既有字典，不改作者内容、SEO、CSS、API、存储协议或依赖。
- 保留原 13 份未跟踪资料和全部历史 worktree，不修改 `public/status/blog-semi-synthetic.json`。
- 不 push、deploy、签名、调用生产模型/DB/relay、发布内容或启用 Feed/Cron；未知 heartbeat 配置保持。
- 依据父路线图 2026-09-08 持续授权及本轮“继续”执行。
