# 公开助手图片错误提示跟随当前语言

## Goal

图片读取或处理失败后，提示始终跟随当前公共界面语言；处理尚未结束时切换语言，最终失败也使用新语言。保留访客草稿、图片归属和显式重试行为。

## Evidence

- 基线 HEAD：`bbc80a4044585431a5d8d856b04f5e8de3a0a1c9`；源文件 506、构建 172、规范 3 与上一轮通过版本一致，原 13 份未跟踪资料保持。
- `image-language-before-valid.json` 在 1440/Morning/中文转英文、320/Stellar/英文转中文，分别覆盖错误出现后切换和图片处理过程中切换，4/4 保留旧语言错误提示。草稿保持，聊天和模型调用 0，页面与外部请求错误 0。
- 证据目录：`C:/Users/zhang/AppData/Local/Temp/blog-semi-assistant-branch-retry-fa3hoffd`。目录名沿用本轮前置评估，不覆盖旧轮次证据。

## Requirements

- R1：四类既有图片错误使用当前语言的既有文案，不新增译文或显示原始异常。
- R2：覆盖已出现错误和异步处理后才出现的错误；切换语言或重开助手不得自动读取图片、恢复会话、发送问题或调用模型。
- R3：保留草稿、模式、会话与分支身份。图片失败结束后释放处理门禁；显式重新选择成功后清除旧错误。
- R4：保留图片请求归属、过期回调隔离、图片不入本地存储等既有契约；不修改解码、压缩、图片大小限制或网络/API 行为。
- R5：保留父路线图的本地授权边界：不 push/deploy/sign，不调用生产模型/DB/relay、不公开内容、启用 Feed/Cron 或修改 scheduler、保护状态快照与 authored/SEO 翻译。

## Acceptance Criteria

- [x] 新增永久回归在旧构建上因语言断言失败，而非夹具准备失败。
- [x] 1440/320/390/430、三主题、两个切换方向覆盖四类错误及异步迟到错误；提示与当前语言一致。
- [x] 语言切换不产生额外请求，草稿/模式/身份保持；失败后可显式重新选图，成功后清除错误。
- [x] 原 48 个图片生命周期场景保持；新增 24 个语言场景通过，共 72。
- [x] lint/build、助手合同、性能、smoke 和最终完整 UI 通过，最终受检输入与构建响应无漂移。
- [ ] 仅按文件白名单本地提交，归档本子任务，实际返回父路线图重新评估并记录会话；原资料保持。

## Notes

- 用户 2026-09-08 的持续本地授权及本轮“继续”覆盖规划、启动、实现、验收和本地提交，不重复审批。
- 前置分支重试探针确认原请求身份保留、4 组均未插入旧问题；不据此改变既有重试契约。初版图片探针未越过会话准备门禁，日志单独保留，不计为产品失败。
