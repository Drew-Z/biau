# 公共语言框架验收

## 结果

- 共享中英文偏好、DOM 语言投影、存储容错、导航辅助名称、页脚、路由载入和 404 已接入。文章、项目及其余页面以 App 的中文标记保留真实内容语言，未声明英文 SEO 或语言 URL。
- 浏览器基址：`http://127.0.0.1:5185`。本地 Windows / PowerShell 7 / Node 24.14.0 / Chromium，未执行远端或生产验证。
- 负向：新检查在旧构建切到 EN 后真实 exit 1，`html.lang` 仍为 `zh-CN`，并非端口/网络失败。
- 静态：lint、TypeScript/build、performance 均真实 exit 0。业务构建后只调整了检查夹具和文档，未再改运行时代码；最终检查器调整后 lint 再次 exit 0。
- 语言专项：12 组（320/390/430/1440 × 三初始主题，每组中英切换）、5 组存储场景、1 组延迟 lazy chunk，真实 exit 0；fixture 限制请求为本地 GET health，modelCalls=0。
- 完整 UI：真实 exit 0，46 组、0 失败、1033914ms。包含博客筛选/576 文字样本、项目分组、阅读恢复、坏地址、状态、助手和其他现有门禁。本轮实际执行，未复用旧轮次构建。
- 全量后仅加强语言夹具：404 布局/截图前恢复并断言矩阵命名的主题。修改后的语言专项再次 12+5+1 全通过；运行时代码、构建、其他检查器 SHA256 与全量快照一致。没有把旧夹具的全量结果说成重新跑了全量。
- 随后 smoke：真实 exit 0，21 组、0 失败、9628ms。无需独立重复全量内已通过的博客、项目和阅读专项。
- 入口 JS 298370 bytes，CSS 152211 bytes，路由 CSS 141623 bytes，预算通过。

## 观察与限制

- 初次正向脚本因 Playwright 的 browser.newPage 独占 context 限制失败，已改显式 context 并关闭；下一次因把 sr-only 当可见文本测量而失败，已从几何检查中排除，辅助名称保留独立断言。这两项是测试夹具问题，不报告为站点回归。
- 最终重新生成并亲自检查了 320px Morning 和 1440px Stellar 截图，英文页脚/404 换行、返回操作和导航可辨认；12 个最终截图的实际主题与名称一致。完整页面截图中的固定底部导航按 viewport 位置呈现，不代表整页无法滚动。
- 当前只完成公共 UI 第一批，目录/详情/状态/AI Daily/助手控件与正文尚待后续阶段，不宣称全站已双语。存储不可用时只保证当前页面选择可用，不承诺无法写入的偏好跨刷新保留。
- 无 push、部署、签名、真实模型请求或公开内容发布；保护快照与既有内容保持不变。

## 证据和清理

结构化哈希、完整检查快照、日志及截图路径见 `delivery-evidence.json`。本轮日志位于系统 Temp 的 `blog-semi-resume-20260908-62seN4`，作为验收资料保留；npm 提示更新未执行。

已确认并删除本会话此前创建且为空的 `ci-port-conflict/blog-semi-site-quality` 及 `ci-port-conflict` 两个 Temp 目录；此前一次性 `verify-site-quality.ps1` 已删除。本轮开始前存在的前导空格 `.trellis` 目录、连续 UI 任务资料、历史 worktree 和不确定归属的旧审计资源继续保留。
