# 公开助手反馈完成的菜单与焦点归属

## 目标

访客提交回答反馈后，可以继续评价其他回答、编辑草稿或查看历史；较早反馈的完成不应关闭新的菜单或夺走当前焦点。

## 已确认问题与依据

- P2：`src/components/PublicAssistantWidget.tsx:1548` 在负反馈成功后无条件关闭菜单并请求原按钮焦点，`:1559` 在失败后也无条件请求原按钮焦点。
- 本地浏览器探针 `feedback-probe.json` 在 1440/Morning/中文和 320/Stellar/英文执行 16 场景。12 个已切换至其他反馈菜单、输入框或 Escape 后输入框的场景全部发生焦点回跳，其中另一菜单遇到旧成功时被关闭；4 个仍停留原菜单的对照场景正常。模型调用为 0。
- 证据目录：`C:\Users\zhang\AppData\Local\Temp\blog-semi-assistant-feedback-7c541gfb`。基线为 `018d28d3446e3f1529619987364956dbafb0f484`，原 13 份未跟踪资料及保护快照已逐字节核对。
- `scripts/check-ui.mjs:3883-3933` 要求当前理由菜单的 Escape、成功、失败及重试恢复其触发按钮焦点；必须保留这些已交付行为。

## 要求

1. 反馈结果仍更新原 Revision 的评分、pending 和错误状态；界面是否继续持有该反馈交互，不能改变请求或评分归属。
2. 旧反馈只能关闭自己发起时的理由菜单；另一条回答的菜单、关闭后重新创建的菜单、历史与新会话不受其影响。
3. 访客仍留在原反馈交互时，保留成功关闭、失败保留及显式重试的焦点恢复。访客已转到草稿、其他反馈控件或历史时，保留其实际焦点。
4. 保留单菜单、Escape、双语、触控目标、会话草稿、Branch/Revision 正文、已存在的请求门禁及固定反馈载荷。

## 验收标准

- [x] AC1：延迟成功和失败均不关闭其他回答菜单、不夺走该菜单或输入框的焦点；草稿可继续输入。
- [x] AC2：原菜单的成功、失败、显式重试及 Escape 行为正常；正反馈继续提交 `helpful`。
- [x] AC3：关闭/重开助手、打开历史、切换历史会话和两条反馈交错完成均保持当前交互归属。
- [x] AC4：持久正文、当前 Branch、会话与草稿不被反馈副作用改写；POST 仅含原 sessionId、revisionId、rating 和固定 reason；模型调用为 0。
- [ ] AC5：四个标准宽度、三种主题及中英文的专项检查，lint/build、相关助手合同、性能、smoke 和完整 UI 全部通过；精确本地提交，仅归档本子任务并实际返回父任务。

## 范围与授权

按父任务 `loop.md` 的持续授权在主会话内完成规划、实施、验证和本地提交。只改反馈交互完成边界及必要检查和规范；不更改反馈 API、模型、数据库、CSS、公开内容、依赖或其他异步动作。不 push、deploy、签名、调用生产模型、启用 Feed/Cron，也不修改 `public/status/blog-semi-synthetic.json`。保留原文件和历史 worktree；不修改未知调度器。
