# 公开助手窄屏输入提示完整可见

## 目标

让 320px 宽度下公开助手的空输入框完整显示现有提示，并保持发送、图片和多行输入可用。

## 已确认问题

- 第 28 轮完整 UI 在 `f529afcb` 上通过 46 组，退出码 0；527 个受检源文件和构建文件未漂移。
- 截图复核后等待助手全屏状态与字体就绪再测量：320px 英文、三主题均为 `clientHeight=57`、`scrollHeight=76`，末行裁切。320px 中文及 360/390/430/1440px 英文对照通过。
- 现有语言检查只核对 placeholder 文本及控件矩形，没有检查输入框内部提示是否完整可见。
- 原始证据：父任务 `round-28-validation.md` 所列目录的 `composer-settled-baseline.json` 与截图。更早的 `composer-baseline.json` 未等待全屏布局稳定，只作诊断过程记录。

## 范围

- 在现有样式中修正窄屏输入框容纳空间，并把空提示可见性加入现有助手浏览器回归。
- 主会话实施及最终验证，使用本地夹具，零真实模型调用。
- 当前路线图的持续本地授权覆盖本子任务的规划、启动、检查和提交。

## 验收

- [x] 新断言先在原构建的 320px 英文助手稳定失败。
- [x] 320/390/430/1440px、三主题、中英文下空提示完整可见。
- [x] 窄屏边界及短视口下输入、发送和图片操作仍可见、可命中，移动按钮至少 44px。
- [x] 长草稿可在输入框内部滚动；语言切换、清空、历史恢复和发送行为保持。
- [x] lint、build、相关助手合同、助手专项、smoke、最终完整 UI、性能与 diff 检查通过。
- [ ] 保护快照和既有用户资料保留；仅本地提交，完成后只归档本子任务并实际返回父任务。

## 不在范围

不修改既有文案、字号、项目/文章/助手 payload、请求与会话逻辑、生产 relay、数据库、公开状态、SEO、依赖或其他任务资料；不推送、部署、发布、开启 Feed/Cron 或改动历史 worktree。
