# Implement: Reference-site Surface Depth And Interaction Integration

1. Add default and six-scene surface material tokens in `src/styles/flow-pages.css`.
2. Apply scene-aware surfaces and depth to `.hero-panel`, `.carousel-wrapper`, and `.carousel-card`.
3. Add a bounded card specular response for hover/focus and a lower-cost mobile treatment.
4. Extend `scripts/check-ui.mjs` to verify six surface signatures, card pseudo response, mobile behavior, and reduced motion.
5. Update the frontend quality spec with the scene-responsive surface contract.
6. Run lint, build, performance, UI, diff, and desktop/mobile screenshot checks.
