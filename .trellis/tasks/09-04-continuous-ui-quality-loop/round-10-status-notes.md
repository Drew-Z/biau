# UI-011：状态入口说明去重

修复日期：2026-09-07。提交前复核：2026-09-08。状态：`fixed / verified`。

## 交付范围

- `src/pages/SiteStatusPage.tsx`：主说明取第一条告警去除首尾空白后的文本，缺失或为空白时回退到原说明；仅渲染非空且不同的次级说明。
- `scripts/check-ui.mjs`：本地状态 fixture 覆盖无告警、不同告警、相同告警、首尾空白及空白告警，断言文案完整、数量、主次样式、可见性和无横向溢出。
- `.trellis/spec/frontend/quality-guidelines.md`：补充回退说明和次级说明的互斥规则与回归矩阵。
- 本记录仅覆盖 UI-011；不修改 CSS、状态数据、路由或链接，不夹带此前未提交的 UI 成果。

## 浏览器证据

三主题与 320/390/430/1440px 共 12 组均从 2 张重复卡片减少为 0 张；所有 7 个入口卡片仍存在。

| 宽度 | 首卡修复前高度 | 修复后高度 |
| --- | --- | --- |
| 320px | 824.23px | 689.86px |
| 390px | 753.44px | 642.16px |
| 430px | 753.44px | 642.16px |
| 1440px | 471.59px | 383.41px |

矩阵使用 reduced-motion，移动启用触控模拟，stellar 切换英文导航。不同告警与背景说明继续同时显示；逐页检查无横向溢出且原入口可点击。证据保留在 `C:\Users\zhang\AppData\Local\Temp\blog-semi-ui-011-20260907-102d85c499a343f4921b4a93f0cb3f65`，不提交截图或浏览器脚本。

## Bug Analysis：重复回退文案

### 1. 根因类别

E（隐含假设）与 D（测试覆盖缺口）：渲染逻辑假设主说明和次级说明不同，忽略了主说明的 fallback 本身就是次级说明；旧检查覆盖路由、操作和布局，但未验证说明段落的数量与去重。

### 2. 修复尝试

首次按渲染根因调整后定向矩阵通过；没有通过 CSS 隐藏、压缩字体或删除公开告警绕过问题。完整 UI 和 smoke 门禁均已通过。

### 3. 防复发措施

- 渲染时统一使用首尾空白归一化后的显示文本比较，原始数据保持不变。
- 明确保留不同告警及补充说明，fixture 通过完整文本和主次样式证明没有误删信息。
- 检查隐藏和缺失节点，不能只比较仍可见段落的重复率。

### 4. 扩展检查

同类搜索仅命中状态总览的此处双段落；状态详情按检查项渲染单条 evidence，不属于相同回退机制。无需抽取跨页面 helper 或改动状态合并逻辑。

### 5. 规范沉淀

已在 frontend quality guidelines 的 Public Content 段落写入可执行显示契约及 fixture 要求。本仓库不是 Trellis 模板源码项目，不创建无关模板目录。

## 验证结果

- 已通过：lint、build、脚本语法、12 组定向浏览器矩阵、status contract（8 项目、7 入口、33 检查项）、性能预算、任务校验与 `git diff --check`。
- 完整 UI：41 组、17 条路由、`failed=0`，554102ms；其中状态说明 fixture 位于 `catalog-reading` 分组。
- Smoke：21 组、7 条路由、3 个 viewport、`failed=0`，10177ms；与完整 UI 均使用隔离预览 `http://127.0.0.1:5183`。
- 性能：CSS `152358 / 222755` bytes，route CSS `141524` bytes，JS `422542 / 430000` bytes；未新增阻塞外部样式表。
- 已删除本轮一次性 `verify-status-notes.mjs`；保留 baseline/fixed 测量、`check-ui.log`、`smoke.log` 与 8 张截图作为证据，不纳入提交。
- 2026-09-08 提交前重跑：lint、build、smoke（21 组、`failed=0`，9703ms）、status contract 和性能预算通过。暂存版检查器语法通过，使用其原始 fixture 和页面助手在当前 production preview 执行 4 组定向回归，7 张卡片、5 类说明情况全部通过；未将这些定向结果冒充一次新的完整 UI 重跑。
- 受保护状态数据、Logo/favicon 与公开数据未改变；用户在四文件提交计划后确认继续，本记录随组件、检查器本轮 61 行和规范本轮 2 行独立本地交付。提交哈希另记入工作区 `implement.md` 与 `ui-audit.md`，不为回写哈希扩大本次提交范围。
- 当前完整工作区包含此前保留的 UI 改动；本轮单独记录与提交，不推送或部署。持续 UI 任务保持 `in_progress`。
