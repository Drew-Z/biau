# 验收记录（2026-09-20）

## 结果

本地修复完成，尚未推送或发布本次补漏。当前开发指令将路线图续作限制为本地交付；此前明确批准的推送已将 5ea0814f 发布成功。本次不重试 Public RAG 同步。

- lint、TypeScript/Vite build、performance:check 全部 exit 0；入口 CSS 151564 bytes，入口 JS 320709 bytes。
- 首页三主题 × 中英文 × 1440/320/390/430：24 配置、98 次背景/阅读采样通过。最低采样 4.7668:1，新增 Stellar 计数场景 7.6398:1。
- 轮播响应/motion 24 cases 通过；smoke 7 路由 × 3 宽度 =21 组，0 失败。
- 后续仅为检查器补 Number.isFinite 防护；运动帧等待先超时，已用138帧坐标查明中心窗口被跳过；按计数实际上下边界捕获真实帧后，最终专项2配置/14采样及lint均exit0。业务构建未变。
- 复看三主题桌面及 Stellar 320px 截图；文字颜色层级、400/500 字重、完整标题摘要、四色边线、124px 桌面卡片与移动自动高度保持。
- 13 项外部修改及依赖/保护状态快照 SHA-256 均未变。所有 UI API 请求在本地截获，真实模型调用 0。

## 负向证据与诊断

1. 旧线上版本普通矩阵第一次在 Stellar 动态计数取得3.47:1；第二轮通过不能覆盖该失败。新增 pointer hover 在旧版稳定以3.18:1失败。
2. 指针光晕隐藏后恢复约6.55:1；实际合成前景的独立颜色样本约3.86:1（不把computed color误当最终前景）。
3. 仅修指针光晕后本地英文计数仍在运动时3.36:1失败。遮罩实测 color/text-fill 均transparent，否定“漏遮”的早期口头推断。
4. 独立运动边光固定到计数旁时3.4105:1，隐藏该层后7.6398:1。最终两层均蒙版到边缘，计数复用现有实色控制底。
5. 新增真实运动帧等待首次孤立实验超时，不记作产品对比度结果；完整矩阵通过；修正等待条件后的两语言专项通过。等待后仅冻结该装饰层当前计算后的paint/geometry一帧，finally恢复，其余背景正常运行。

## 版本与限制

旧已推送版本5ea0814fe978300229fb375ae16c2f3b71356132：稳定Pages域和部署预览HTML、6份入口资源哈希一致；Site Quality run35410546715成功，Pages成功。Public RAG sync run35410546708在就绪门禁等待约15分钟后失败，尚未发出同步POST；日志不足以区分后端commit/checksum或健康响应异常。

本次本地 build 包含外部尚未提交的9项目数据，提交白名单仅包含样式/组件绘制投影/检查器/规范/任务记录，不包含这些数据。线上仍是8项目版本；本地验收不等于未来精确提交的线上验收。未运行本版本全站46组完整UI；本轮采用受影响首页矩阵、motion、smoke与构建检查，不冒用历史全量结果。未来推送后应执行现有14组production appearance及带业务API拦截的首页专项。当前不保证所有未来动画帧或所有未覆盖浏览器。

## 证据与恢复

证据：D:/Agent/codex/visualizations/2026/09/12/01a0952f-555f-7c70-a491-ba1840e626a3/release-verification-2026-09-19-2026-09-19T05-27-57-101Z。关键目录 local-final（98采样）、local-stellar-verified（最终检查器）、orbit-diagnosis（第二层负向）、resume-diagnosis-2026-09-20（遮罩确认）；原始失败均保留。

恢复组：D:/Agent/codex/backups/tasks/2026-09-19T06-03-54-022Z-stage-4-stellar-edge-glow，manifest.json记录基线SHA/文件哈希与实体快照。

额外验证：现有 check:ui:production-appearance 对本地最终构建运行14组/0失败（base为127.0.0.1:5199），覆盖三主题、键盘持久化、默认主题和三个移动宽度。此结果不是新版线上验收。
