# AI Daily production operations implementation

## Ordered Work

- [x] Finalize environment-variable and secret inventory.
- [x] Add Render build/start/schedule documentation for both Cron Jobs.
- [x] Add production-readiness configuration command with no network calls.
- [x] Curate sources/query groups and implement the approved initial model-selection path. (site owner confirmed the 2026-07-19 source pre-review; the 2026-07-23 model decision uses a zero-call static mapping with explicit reduced redundancy, while measured evaluation remains optional rather than a production prerequisite)
- [ ] Configure metrics, diagnostics, retention, and alerts. (low-sensitive diagnostics/metrics, six-category failure projection, repository Grafana/Prometheus assets, offline contract checks, and guarded retention dry-run completed; retention mutation plus production scrape/import/notification routing remain)
- [x] Run the full deterministic quality gate.
- [x] Approve and deliver the manual static-selection bundle.
- [x] Run one explicitly confirmed real edition attempt and two approved same-Edition reruns; all remained rejected/degraded and did not produce a draft. After the substantive relay-token repair, the latest approved Run `cmsqdx5bp000045j2q7rh3mxj` completed with gaps and revision 4 was rejected/discarded at the extractor/provider boundary.
- [ ] Review/approve selected flashes and the daily draft, export, deploy, and verify public behavior.
- [x] Record final runbook, manual gates, and sealed rollback evidence for the failed acceptance attempt.
- [ ] Deliver the Free3 dedicated fixed relay, replace the AI Daily runtime/approval identity, and obtain a fresh explicit approval before one further real Edition.

## Local Slice Completed

- `AI_DAILY_TIME_ZONE` now derives the edition date in `Asia/Shanghai` by default.
- Ingest deadlines reuse the bounded interval-minus-two-minutes policy and remain shorter than the feed cadence.
- `AI_DAILY_PUBLIC_FEED_ENABLED` is fail-closed by default; the Render blueprint keeps it disabled until the manual public-feed gate passes.
- `ai-daily:production-readiness-check` reports repository readiness and manual gates without network calls.
- `ai-daily:contracts-check` runs the fixture/loopback contract suite; disposable PostgreSQL checks require explicit `--with-database` and `AI_DAILY_DATABASE_CHECK=1`.
- The full production-preview UI gate passes after the flow worker gained a token-correlated reduced-motion acknowledgement instead of relying on fixed test delays.
- Runtime reduced-motion synchronization now also re-reads the current media query through a change-only fallback poll, accepts only current/equivalent worker acknowledgements, and lets the compositor present two frames after acknowledgement before pixel stability sampling. This closes the production-preview race without weakening the static-frame threshold.
- Manual static provider selection and bundle delivery are complete. Production Cron activation and live edition acceptance remain manual gates; source/query curation is approved with a bounded enabled subset, and the Studio database deployment gate is complete.
- Studio now exposes a token-protected read-only AI Daily operations snapshot; Studio `/metrics` appends the same low-cardinality data when metrics are explicitly enabled.
- Source health, run/stage state, work backlog/expired leases, bounded quality outcomes, public feed age, and retention-due counts are covered by `ai-daily:operations-check` without a database or network.
- Operations now projects fixed `config` / `provider` / `evidence` / `quality` / `infrastructure` / `stale-content` signals, exposes the low-cardinality `biau_ai_daily_failure_signals` gauge, and includes repository-owned Grafana dashboard and Prometheus alert templates validated by `ai-daily:observability-contract-check` with no network calls.
- Retention now has a Studio-authenticated, bounded `retention-dry-run-v1` plan with fixed eligibility/block reasons and no mutation path. `ai-daily:retention-check` proves current evidence, public/current-approved Flash, revision history, and approval audit history remain protected.
- `server/data/ai-daily-source-manifest.v1.json` now records 33 reviewed sources and 10 reviewed discovery query groups, with 18/4 approved entries enabled and hold/rejected entries disabled. The 2026-07-26 follow-up approved OpenAI News RSS and Google AI category RSS, while Microsoft AI's archived feed moved to rejected. The 2026-07-27 audit registered Apple Machine Learning Research and IBM Research as new disabled hold sources, moved Google Cloud AI/ML to its verified RSS while retaining hold, and moved Hugging Face to its verified RSS while retaining the author-mix hold. `ai-daily:manifest-check` validates schema, public URL/domain rules, unique canonical identity, query budgets, review thresholds, enabled/review consistency, and those four reviewed feed identities without network calls.
- `ai-daily:model-evaluation-check` now validates the 40-case fixture contract for extractor/composer/verifier selection, recomputed case-set hashes, existing quality floors, deterministic ranking, independent failure-domain fallbacks, low-sensitive immutable records, and explicit human approval. It performs zero provider calls and proves only the optional measured-evaluation contract; it is not a production prerequisite for the manual static-selection path.
- `docs/ai-daily-source-review-2026-07-19.md` records the public-page source/query pre-review, owner confirmation, extraction and attribution boundaries, and the remaining model-selection/first-edition gates. No model was called by the approval transition.
- The production model slice now has a server-only runtime config, a Responses-only structured provider without `temperature`, serial three-role business evaluation, tamper-evident proposal/approval artifacts, runtime channel drift checks, and mutually exclusive `--fixture`/`--live` runner profiles. Runtime schema v2 requires `protocol: "responses"`; all deterministic checks use loopback or fixture providers and make zero external calls. The real evaluator supports an explicit `--allow-reduced-redundancy` comparison mode for multiple models exposed by one provider; it never relabels that shared failure domain as independent failover.
- Model evaluation now reads a versioned 30-case golden asset with six required categories and eight required negative slices. Candidate/report schemas bind category and sorted negative tags, business profiles must match the repository case set, and per-slice floors prevent a weak hallucination/citation class from being hidden by the global average. The evaluation/proposal/bundle schemas moved to v2; no real model was called by this migration.
- Business execution evidence now binds `resultSetHash` to the canonical SHA-256 of the complete measured case array; a format-valid stale or substituted hash is rejected with `business-result-set-hash-mismatch`, with a regression test covering post-hash case tampering. No real model was called by this change.
- Golden descriptor versions now carry a normalized full-case content fingerprint, so scenario/outcome/score drift invalidates old proposals even without a manual version bump. Extractor, composer, and verifier also inject and assert every declared negative-tag challenge before a business case can be recorded. Deterministic checks cover both contracts with zero provider calls.
- Production approval delivery now uses an explicit Render Secret File contract: Studio and every future Editorial Cron that executes live work each point `AI_DAILY_MODEL_APPROVAL_FILE` at `/etc/secrets/ai-daily-model-approval.v1.json`, bind the same human-approved canonical hash through `AI_DAILY_MODEL_APPROVAL_BUNDLE_HASH`, and validate file integrity plus runtime identity with `ai-daily:model-approval-check` before live work. The real per-service file upload and hash value remain a human gate; Ingest Cron receives no model credentials.
- The first-edition acceptance manifest contract now binds the approved proposal/bundle, `PRODUCTION` issue/run/date, Studio review and draft version, Publish Export checks, five deployment observations, and a sealed rollback-evidence reference. `ai-daily:acceptance-check` covers hash tampering, fixture rejection, cross-stage mismatches, failed export/deployment, rollback evidence missing/binding/reference/tamper cases, sensitive fields, and old schema with zero provider calls; `ai-daily:acceptance` provides `init`, `check`, and `seal` for the Git-ignored local record. Production readiness reports the acceptance and rollback contracts as pass and each real sealed record as a separate manual gate.
- The Studio Render Blueprint now explicitly keeps both metrics switches disabled, and the deployment contract prevents either default from drifting. The Prometheus template raises a dedicated critical alert when the AI Daily operations snapshot remains unavailable for five minutes; the observability contract verifies that rule separately from the six failure categories.
- Studio mobile workspace tabs now use a complete roving-tabindex interaction model: Arrow keys, Home, and End move selection and focus, while each desktop/mobile panel keeps a stable `aria-labelledby` relationship. The production-preview UI contract covers the keyboard paths at 320, 390, and 430px without changing the desktop three-column layout.
- The repository Grafana dashboard now exposes the existing low-cardinality source health, work queue, freshness/lag, provider-role, issue-lifecycle, public-content, and retention-due gauges; the observability contract checks every panel expression without requiring a datasource.
- Nullable freshness, end-to-end lag, and public-feed age now expose explicit availability gauges; dashboard queries filter on those gauges so missing checkpoints cannot render as fresh zero-minute values. Dashboard contracts also validate YAML/JSON structure, unique panel IDs, bounded grid layout, target refIds, and non-overlapping panels.
- Rollback evidence is now a separate low-sensitive sealed manifest. Acceptance v3 records `selectionBasis`, binds the evidence id/hash and the same acceptance/edition/issue/run tuple, and rejects mixed artifact families; no local check claims that Render or Cloudflare actions happened, and missing real evidence remains a human gate.
- On 2026-08-12, commit `2566ac15` deployed the substantive generation repair: AI Daily now reuses the shared Responses request boundary with role-specific strict Structured Outputs schemas, and prompt behavior is versioned as `ai-daily-prompt-v4`. Runtime/provider/composition/26-contract, server build, lint, Vite build, rollback-sealed, and sensitive-scan checks passed with zero provider calls. Render `/health` returned `200`; unauthenticated Studio health returned `401 missing-studio-token`; the public Feed remained disabled. A new approval bundle/Secret File/hash and a fresh explicitly approved real Edition are still manual gates.
- On 2026-08-13, manual static-selection artifacts moved to v2 and now bind `ai-daily-prompt-v4` plus `ai-daily-generation-v2`. Approval and production provider construction share the same current-generation contract check; a correctly rehashed stale prompt/schema proposal is rejected before approval, and a stale approved bundle is rejected before provider construction or database claim. A validated legacy v1 pending proposal may be explicitly upgraded into a new pending v2 proposal, but a v1 approved bundle cannot be upgraded or loaded. The zero-call local reapproval produced proposal hash `996a04e36f8e5bdeec1b1a55683b603aaf361b04bf59744250c71eb14aa8777c` and bundle hash `1b27f23f5b4871757a0a12e677fcc36edd3c606bf4d9124e069e69e2a4161a3f` for selection `ai-daily-grok45-static-v2`, with all three roles mapped to `grok-4.5` and truthfully labeled `reduced_redundancy`. The Render Secret File/hash still require an external control-plane update before production delivery validation can pass; generation, business evaluation, public Feed, and Cron remain disabled.
- The latest approved Run `cmsqdx5bp000045j2q7rh3mxj` used work item `cmsqdx5ce000145j28q2avapi` and ended `COMPLETED_WITH_GAPS`; revision 4 remained `REJECTED` / `DISCARDED` with eight citation snapshots and no draft or public content. Cloudflare's matching call returned the fixed `model-relay-upstream-unreachable` envelope after about eight seconds, narrowing the current blocker to the original relay channel's upstream transport. The owner then supplied a separately configured, stable `Free3` Responses channel. The repair adds `/api/model-relay/free3/responses` with its own Secret pair and binds the future AI Daily runtime to `free3-relay` / `free3-channel` only; it does not overwrite either existing relay route and does not auto-fail over on transport failure. Because the provider/failure-domain identity changes, the current approved bundle is ineligible and a new static proposal/bundle plus a new explicit real-Edition approval are required.
- Free3 relay commit `2ccd44c8` passed the fixed-upstream relay, public Responses adapter, runtime/provider, 26-contract, deployment-contract, server build, lint, Vite build, diff, and exact-secret scans with zero external provider calls. Cloudflare Pages production deployment `adcc0f4b-675f-4531-af08-10c66259fe8c` is active with the two route-specific encrypted Secrets; unauthenticated `/api/model-relay/free3/responses` returns `401 model-relay-unauthorized` from `pages_function`. An authenticated invalid-model request from the local Python HTTP stack was blocked by Cloudflare edge as plain `403` before the Function, so the production `400` boundary is not claimed; the deterministic fixture proves that boundary locally with zero fetches. Studio deploy `dep-d9umcqtbedkc73aijrg0` is live on the same commit, health/database/auth passed, unauthorized workspace returned `401`, public Feed remained `404`, and the deploy window contained no error-level log. Generation, business evaluation, and public Feed remain false; Cron remains absent.
- Zero-call manual selection `ai-daily-free3-grok45-static-v2` is pending with proposal hash `708108cf06ea92d1e9cf8f8d15441e36134ee5d465695e4e2cac840b5f11c740`. Extractor/composer/verifier map to `extractor-free3-grok45`, `composer-free3-grok45`, and `verifier-free3-grok45`; every role uses `grok-4.5`, `free3-relay`, and `free3-channel`, remains `reduced_redundancy`, and retains no endpoint or credential. Explicit proposal approval is required before bundle creation or Render runtime/Secret File/hash delivery; a later real Edition still requires a separate explicit approval.
- Render deploy `dep-d9u9o3942hec739tc9g0` put approval-contract commit `072795a8` live on the Studio service. `/health` returned `200` with `database=true` and `authConfigured=true`; the unauthenticated Studio workspace returned `401 missing-studio-token`, the public Feed returned `404`, and the deployment window contained no error-level build/app logs. This deploy changed code only: it did not upload the new v2 Secret File, change the configured bundle hash, enable generation/business evaluation/public Feed, create Cron, or call a model.
- On 2026-07-23 the site owner accepted skipping a logical backup because the Supabase Free project had no platform backup, contained no judged-irreplaceable AI Daily data, and the pending migrations were additive. Render revision `76c23cd` was retained as the application rollback point; `3466eac7` then applied the AI Daily schema chain through `20260719020000_ai_daily_public_feed_index`, and the public Studio `/health` returned `200` with database and auth readiness true. No model was called.
- On 2026-07-23 a user-approved same-provider comparison was started, then stopped at `extractor-grok45 20/30` when the owner clarified that catalog-name selection was sufficient and exhaustive multi-model availability testing was not wanted. No proposal, approval bundle, or persisted evaluation result was produced. The static initial role mapping is `qwen3.7-max-t` for extractor/verifier and `grok-4.5` for composer; the remaining catalog models stay unconfigured and uncalled.
- Static selection now has its own pending proposal and approved bundle schemas plus `ai-daily:model-select` / `ai-daily:model-select-approve` CLIs. Both commands require explicit reduced-redundancy acknowledgement, call no provider, retain no endpoint/key/prompt/output/metrics, and bind exactly one runtime candidate to each role. Production loading, delivery checks, runtime drift validation, and acceptance v3 accept either this manual path or the optional measured-evaluation path and expose `selectionBasis` truthfully.
- Studio now exposes an authenticated, explicit-confirmation live-run action. It validates the Edition version and evidence floor, reuses the CLI production execution resolver, queues durable `PRODUCTION` work, returns `202`, and wakes a lease-fenced polling worker. The workspace projects only `ready` / `disabled` / `misconfigured` readiness and auto-refreshes active runs; no channel secret, endpoint, raw provider response, or health prompt crosses the browser boundary.
- The first online read-only verification of commit `18818c8d` found that the workspace Prisma query still used the obsolete relation alias `overrides` while the schema exposes `editorialOverrides`. Health, auth, issue detail, operations, drafts, and export endpoints remained `200`, while only the workspace returned a sanitized `500`. The query now uses `editorialOverrides`, maps it back to the stable DTO field `overrides`, and is guarded by `satisfies Prisma.AiDailyRunInclude` after a fresh `prisma generate`.
- No delete/archive path was added. Explicit mutation design, production alert routing, backups, and rollback validation remain follow-up work.

## Latest Runtime Hardening

- The first deployed v6 acceptance run (`cms0pq5bl000145e3mkxbiu78`) confirmed the exact-token and topic-intent relevance repair in production. It completed four discovery groups plus two due feeds with 24 candidates; the F1 Kimi result remained unchecked, and non-news/low-value opinion content stayed outside the eight provisional selections. The run correctly stopped with `minimum-tier1-sources-not-met` and `tier1-collection-stale`, so production generation remained disabled and no model was called. The audit also exposed that the Edition still retained its earlier v5 selection after the v6 run failed closed. Studio live-run authorization now requires the latest `DEGRADED` ingestion run to match the current config version and finish as `COMPLETED`; missing, stale, active, failed, or completed-with-gaps runs return bounded evidence-not-ready reasons before any generation work is queued.
- The first deployed v5 run proved the provider-specific query repair: The News API contributed 20 candidates, 26 of 30 candidates reached ready evidence, and deterministic selection cleared its structural floors with eight events. Manual content review then caught a separate ranking defect before generation was enabled: internal topic labels were included in `aiRelevanceScore`, the raw substring `ai` matched words such as `daily`, and no independent relevance floor existed. Runtime v6 removes topic labels from evidence, uses exact AI identity terms plus query-group-specific intent vocabulary, and requires a minimum AI relevance score in addition to the unchanged total, event, domain, and Tier 1 floors. This intentionally allows a structurally healthy run to stop with evidence gaps when its Tier 1 items are not actually AI news.
- The deployed v4 replay completed all four discovery groups, but The News API still produced zero normalized candidates after the source-category filter was removed. This isolated the remaining defect to query semantics: long provider-neutral prose was being passed directly to an API whose documented advanced search contract uses explicit `+`, `|`, quotes, and parentheses. Runtime v5 adds bounded `providerQueries.theNewsApi` expressions to each enabled curated group while keeping GDELT/HN on provider-neutral queries; manifest validation rejects enabled groups without the provider DSL. The same production sequence also exposed a zero-work refresh that remained `RUNNING` forever because no work item could invoke finalization; empty runs now finalize immediately. No selection floor changed and no model was called.
- The first deployed `ai-daily-ingestion-runner-v3` run completed five due feed items and all four discovery groups. It produced nine candidates (seven `READY`, one `THIN`, one `UNCHECKED`) but selected zero events, so the evidence gate correctly retained all three `minimum-events` / `minimum-distinct-domains` / `minimum-tier1-sources` gaps and production generation remained disabled. The News API `/v1/news/all` requests succeeded but returned zero normalized candidates in every group; GDELT contributed one lead and failed in other groups, while HotDaily contributed bounded signal leads. No model was called.
- That run also proved the publication-date repair: four undated Cohere listing links resolved to evidence dates in May, June, and 14 July and remained ineligible for the current 36-hour window. The remaining acquisition defect was The News API's `categories=tech,science` filter. The provider documents categories as a property of the publishing source, not the individual article, so relevant AI coverage on general or business sources was being excluded before evidence review. Runtime v4 removes only this source-category filter; curated queries, language/date/domain bounds, original-page evidence, ranking, and all selection floors remain unchanged. A fixture assertion prevents the filter from returning, and the config-version bump allows one bounded same-bucket production replay after deployment.
- The first post-fix business collection for the `2026-07-26` edition completed all 16 feed and four discovery work items without a fetch failure. It produced ten candidates (nine `READY`, one `THIN`) and no longer reported the historical Tier 1 P95 gap, proving the previous freshness fix in production. Selection still stopped correctly with two simulated events and `minimum-events-not-met` / `minimum-distinct-domains-not-met`; production generation stayed disabled and no model was called.
- The run-level audit showed that the remaining gap was candidate structure, not dedupe: high-score eligible content was concentrated in two feeds, undated Cohere listing links resolved to older full articles, and The News API returned zero candidates from the Top Stories endpoint. The adapter now uses the provider's `/v1/news/all` search endpoint. Direct evidence extracts bounded JSON-LD `datePublished`, but only an in-window date may promote `leadOnly`; old complete articles remain visible and ineligible rather than being mislabeled as today's news.
- Discovery idempotency now includes `ai-daily-ingestion-runner-v3`. This allows one bounded same-bucket replay after the acquisition contract change while preserving no-duplicate behavior for repeated refreshes on the same deployed version.
- The first completed production run showed two acquisition defects rather than a model-quality failure: GDELT dates used an ISO-derived form that its DOC API rejects, and The News API spent only one request regardless of the curated query budget. GDELT now emits `YYYYMMDDHHMMSS`; The News API rotates through at most `maxRequests` distinct queries, merges/deduplicates bounded results, and uses `zh,en` for Chinese coverage. Deterministic fixtures assert the exact request shape without a provider call.
- Tier 1 discovery P95 no longer treats every article in the 36-hour editorial lookback as a newly published item. Only Tier 1 candidates published on or after the durable run `startedAt` enter the current-run P95; historical evidence remains visible through collection age, evidence freshness, and end-to-end lag. The freshness fixture covers the separation.
- Studio run events now expose a low-sensitive discovery/evidence diagnostic
  summary instead of dropping all persisted context or returning raw
  `metadataJson`. The summary is allowlisted to fixed provider ids/slots/
  outcomes, bounded counts, stable gaps, and stable error categories; unknown
  providers become `unknown-provider`, and endpoint/token/raw-response fields
  are regression-tested as absent. The Runs and Candidates views render these
  diagnostics responsively, so an existing completed run can distinguish zero
  results, provider failure, and evidence gaps after deployment without another
  provider or model call.
- Re-ran the post-review deterministic gates after the ingestion runner changes: evidence, Studio workspace, service-mode smoke, full AI Daily contracts, server build, lint, production build, UI check (19 routes x 2 viewports), performance budget, and `git diff --check` all pass. No model, search, extraction provider, or liveness request was made.
- Manifest synchronization now preserves learned `ETag` and `Last-Modified` validators instead of resetting them on every refresh. Conditional requests therefore remain effective across manual refreshes and process restarts.
- Ingestion candidate selection is bounded to four normalized candidates per feed and prioritizes date-bearing entries with a stable canonical-key tie-break before spending evidence-fetch budget on undated leads.
- The generic ingestion drain now finalizes every run id it touched, not only the last run in a batch; the legacy single `finalization` field remains for callers that process one run.
- The remaining production gates are intentionally human-controlled: run one confirmed real edition, review and export it, observe deployment/public behavior, seal rollback/acceptance evidence, then decide on Cron/public-feed activation. No readiness check can claim those actions happened.
- The first production ingestion attempt on 2026-07-25 synchronized all 30 feeds and queued the expected 16 enabled feeds, but every source failed immediately with `network_error` and zero candidates. A local real-source reproduction exposed the low-level cause: Node 22/24 invokes custom socket lookup with `options.all=true`, while the pinned SSRF-safe transport returned the legacy scalar callback form, producing `ERR_INVALID_IP_ADDRESS: undefined` before the HTTP request. `createAiDailyPinnedLookup()` now supports both callback contracts; the deterministic evidence check covers both branches, and the same approved Anthropic source fetch succeeds locally after the fix. No model or search provider was called.
- The production discovery path no longer depends on Tavily: official RSS/Atom sources are supplemented by optional The News API primary, GDELT fallback/no-key primary, HN Algolia, and HotDaily. HotDaily is a cached signal-only adapter and drops every generated summary, score, reason, and trend field before persistence; all non-official leads require original-page evidence. Discovery groups are durable `DISCOVER` work items and run finalization waits for both feed and discovery work. Deterministic provider fixtures make zero network or model calls.

## Known Follow-up

## 2026-07-26 Production Cohort Repair

- Production refresh `cms0qb5t2000145b6c06pl80z` used `ai-daily-ingestion-runner-v6`, processed 14 due Tier 1 feeds, and completed with gaps. Thirteen feeds succeeded; Alibaba Group News remained `evidence_rejected`. Six retained candidates were historical or not relevant to the current AI/cloud-native edition, so the run selected zero events and correctly reported the event/domain/Tier 1 minimum gaps. No model was called.
- The preceding same-Edition V6 run `cms0pq5bl000145e3mkxbiu78` already contained four discovery groups and 24 candidates, but the next refresh could not enqueue them again because discovery work is idempotent inside its six-hour bucket. The former run-local finalizer therefore evaluated discovery and due official feeds separately even though both belonged to the same Edition and ranking contract.
- The repair treats at most 48 non-failed `DEGRADED` runs and 480 candidates with the same `issueId + aiDailyIngestionConfigVersion` as one bounded evidence cohort. It merges the earliest start and latest freshness checkpoints, reranks the combined candidates, allows an earlier cohort representative, and rebinds that representative to the current decision run's persisted cluster.
- Generation authorization now follows the actual persisted selection authority. All selected evidence must resolve to one current-config, completed, same-Edition `DEGRADED` decision run; a later empty maintenance run cannot invalidate a valid selection, and a current maintenance run cannot launder an older-config selection.
- Deterministic server build, Studio production, ranking, 26-contract aggregate, lint, Vite build, Prisma validation, and diff checks passed before commit. The database-backed repository assertion remains intentionally unrun because no disposable PostgreSQL database ending in `_test` is configured; it must never be pointed at production Supabase.
- A post-deployment read-only check confirmed the Studio and dedicated database are healthy, production generation remains disabled, and the Edition is still `needs-more-evidence` on the pre-repair deployment. The production cohort behavior therefore still requires one deployment of this commit followed by one bounded ingestion refresh; no model or generation action is authorized by that refresh.
- The deployed Studio revision includes the cohort repair. One explicitly approved bounded refresh for the `2026-07-26` Edition created run `cms1wfw8700013miebdubde01`, processed 16 enabled feeds plus four discovery groups, and terminated as `completed-with-gaps` with 26 candidates and 40 cohort clusters. Fifteen feed jobs succeeded; Alibaba Group News remained `evidence_rejected`.
- The final evidence gate reported only `minimum-tier1-sources-not-met`. The top ten current-window clusters were Tier 2 discovery results, while visible Tier 1 candidates were outside the news window and remained ineligible. Production generation stayed disabled and no model was called. Do not force stale Tier 1 items into selection or lower the evidence floor merely to produce the first Edition; the next step is an explicit editorial choice to wait for fresh official evidence or separately review additional Tier 1 feeds.
- Studio deploy `dep-d9j249jtqb8s739l603g` put source-expansion commit `887959ac` live on 2026-07-26. `/health` returned `200` with Studio database and auth readiness true. The follow-up bounded refresh created run `cms1xs4os000145atpu4i14w5`, synchronized the 31-source manifest, queued all 18 enabled feeds, and terminated as `completed-with-gaps`. OpenAI News and Google AI RSS both collected successfully but yielded zero entries inside the 36-hour publication window; Cohere contributed four ready historical candidates, and Alibaba Group News remained the only failed feed with `evidence_rejected`.
- The expanded-source run reranked the 40-run cohort and retained ten selections, but its only final gap remained `minimum-tier1-sources-not-met`. This confirms the new feeds are operational without manufacturing freshness: production generation remains disabled, no model was called, and a real Edition must wait for independently fresh Tier 1 evidence or a separately reviewed source expansion.
- A follow-up source audit found the enabled Alibaba Group HTML page was the sole `evidence_rejected` feed even though Alibaba-owned Alizila exposes a stable dated RSS stream. The manifest now points the same Tier 1 source identity at `https://www.alizila.com/feed/`; this change is locally schema-checked and awaits one bounded post-deploy refresh before its operational effect is recorded.
- Studio deploy `dep-d9j2eljrjlhs73fq23l0` put the Alizila source repair (`a102fb43`) live. Bounded refresh `cms1yeuly000145hn4fln1m63` processed all 16 feeds due in that interval with no failed work item, four ready historical candidates, and 40 cohort clusters. Operations now reports all 18 enabled sources `HEALTHY`, zero source failures, zero ready backlog, and zero expired leases.
- The repaired run still terminated `completed-with-gaps` with ten retained selections and the single `minimum-tier1-sources-not-met` gap. Production generation remained disabled and no model was called. At that point, one pre-fix v3 zero-work run (`cms0njgeg001z3mi2bql1pmm9`) was still historically marked `RUNNING`; it did not own work or block the current v6 cohort, but a targeted production data reconciliation required a separate explicit mutation decision.
- The site owner approved a targeted reconciliation of that zero-work v3 record. A guarded atomic production update moved it from `RUNNING` to `CANCELLED`, set its finish time, advanced its event sequence, and appended one low-sensitive `run-reconciled / cancelled` audit event. Post-checks showed zero running runs and one cancelled run; production generation remained disabled and no model was called.
- That reconciliation exposed an observability ordering defect: the operations snapshot selected its latest run by mutable `updatedAt`, so auditing the old v3 record temporarily displaced the newer v6 result. Latest-run status, stage, freshness, lag, and alerts now use immutable `createdAt DESC, id DESC`; the deterministic operations check locks this contract so later reconciliation updates cannot rewrite operational chronology.
- Studio deploy `dep-d9j2pnbrjlhs73fqj430` put chronology repair commit `50190c01` live. `/health` confirmed database and authentication readiness, and the authenticated operations snapshot restored the actual latest v6 result as `COMPLETED_WITH_GAPS / RANK` with pipeline freshness and end-to-end lag available. It also reported `RUNNING=0`, `CANCELLED=1`, all 18 enabled sources `HEALTHY`, zero ready backlog, zero expired leases, and no config/provider/infrastructure/stale-content signal. The remaining degraded state is the existing evidence warning; production generation remains disabled and no model was called.
- A new bounded refresh for the `2026-07-27` Edition queued 18 due sources plus four discovery groups. All 22 work items succeeded with zero source failure; 23 of 24 candidates reached ready evidence. Deterministic ranking found seven provisional selections but correctly persisted none because the only final gap remained `minimum-tier1-sources-not-met`; visible Tier 1 candidates were older Cohere pages outside the 36-hour window. Production generation stayed disabled and no model was called.
- Direct public-RSS review then verified Apple Machine Learning Research, Google Cloud AI/ML, IBM Research, and the existing Hugging Face feed. The first three are now registered as disabled hold candidates pending explicit site-owner approval; Hugging Face remains hold until author/organization filtering exists. Their latest entries were already outside the current Edition window, so the review expands future coverage without pretending to repair the current evidence gate.
- The site owner then corrected the business policy after comparing HotDaily, TrendRadar, and NewsNow acquisition patterns: a daily edition must not require two vendors to publish fresh official posts. Ingestion v7 removes only the fixed Edition-wide Tier 1 quota while retaining official authority scoring, five-event/three-domain/original-page readiness, and all freshness/relevance gates. Studio and the generation worker now share the same minimum evidence helper; release/date/price/API/availability claims still require `official` evidence, and cross-event trend blocks require at least two independent publisher domains. The three newly reviewed official feeds remain disabled hold supplements rather than first-edition blockers. No provider or model was called by this policy correction.
- Studio deploy `dep-d9j4497avr4c73br3f3g` put policy-correction commit `c37663ac` live. `/health` returned the expected Studio identity with database and authentication ready, and the post-deploy error log was empty. The explicitly approved bounded v7 refresh created ingestion run `cms22j9kh000145gpya0d2fxl`, queued 18 feeds plus four discovery groups, and completed at `PROMOTE` with zero active work, retry wait, or expired lease. It persisted seven selected candidates/clusters for Edition `cms1zpb3f000045ky0pvzifps` (`selectionVersion=1`, `selectedEvidenceVersion=2`, workflow `evidence-ready`). The observed `fallback-invalid_response` discovery diagnostic did not become a repository evidence gap or block selection. No model was called.
- The site owner then explicitly approved the first real Edition. Production generation was enabled only for the run window and disabled immediately after terminal state. Run `cms235pnc000045hgo0syaq4z` made one extractor call through the approved `qwen3.7-max-t` mapping, failed with the bounded but overly broad `provider_error`, persisted a rejected/discarded revision, and created no draft or public content. After explicit owner approval, a replacement manual-static bundle mapped extractor/composer/verifier to `grok-4.5`; its offline approval check passed with zero network/model calls, the Studio Secret File and runtime mapping were updated, and production readiness returned `ready`. Retry run `cms23vuf1000045gkhpi8uz4d` again made exactly one extractor call and failed with the same broad `provider_error`, proving the blocker belongs to the shared Responses channel/request boundary rather than the original Qwen model selection. The generation flag was again disabled immediately, and no draft or public content was created.
- Provider failure checkpoints now preserve a fixed low-sensitive category for request invalid, auth, rate limit, unsupported endpoint, upstream, timeout, network, empty response, invalid JSON, payload too large, or generic provider failure. Unknown checkpoint categories fail restoration. The change does not log endpoints, response bodies, credentials, exceptions, prompts, or model output, and it does not alter request or fallback behavior. A further real call requires a new explicit owner approval after this diagnostic build is deployed.
- Diagnostic commit `7c54b87b` was deployed as Render revision `dep-d9j529urnols7387ile0`. The service reached `live`, `/health` and the authenticated AI Daily workspace returned `200`, and production generation remained `disabled`; deployment verification made no provider/model call. At that point, any real Edition retry remained a separate explicit owner gate.

- The only approved same-Edition rerun after prompt-validator v3 deployment used Issue `cmspekr1d000044bmbbnjin5u`, Run `cmspkl33d000045c7w21irnvy`, and Work item `cmspkl341000145c760bfj2rx`. The HTTP entry returned `202`; the terminal outcome was `COMPLETED_WITH_GAPS` with `attemptNumber=3`. Checkpoints reached `EXTRACT_FACTS`, `VALIDATE`, and `DRAFT`. Revision 2 recorded `promptVersion=ai-daily-prompt-v3`, `validationStatus=REJECTED`, `applyState=DISCARDED`, eight citation snapshots, and the critical `extractor-schema-or-provider-failure` finding. No draft, Studio review, Publish Export, deployment, or public Feed was created.
- Rollback evidence `tidebrief-rollback-2026-08-12` is now sealed. The custom-format database dump passed an independent restore test; the Git-ignored local record preserves the existing acceptance binding and records the previous Render revision plus 19 applied migration names. `npm.cmd run ai-daily:rollback -- check --require-sealed` passes, but this rollback seal does not claim that the latest Run has a final acceptance seal. Generation, public Feed, business evaluation, and Cron remain disabled/not created. A further real provider call is prohibited until a new substantive production fix is deployed and explicitly approved.

- The frontend performance follow-up is now closed by the archived `07-19-frontend-css-budget-cleanup` task. Current production output passes at `236074 / 240000` CSS bytes and `354307 / 430000` JS bytes.

## Production Slice Status

- Implemented: runtime channel parsing, Responses provider adapter, optional serial business evaluator, manual static-selection and measured approval artifacts, production provider binding, runner profile isolation, Secret File/hash delivery contract, acceptance manifest/CLI and tamper contract, readiness reporting, and deployment/manual-gate documentation.
- Still manual: complete a substantive production repair, obtain a new explicit approval, and only then consider another bounded real Edition. The latest approved rerun is `COMPLETED_WITH_GAPS`/`REJECTED`, with no draft, review, export, deployment, or public Feed; generation/public Feed/business evaluation remain disabled and Cron is not created. The sealed rollback evidence is complete. After a future successful Edition, fill and seal the acceptance manifest after Studio review/export/deployment observation, and only then consider Cron/public-feed activation. Ingestion v7 is deployed and its first bounded refresh has persisted a valid seven-item selection. The static selection proposal, approved bundle, runtime JSON, Secret File, hash, and Studio deployment are complete. Measured evaluation is optional and only runs when a future quality-comparison or independent-fallback decision requires it.
- Guardrail: no automated health check or deploy hook invokes a model; no fixture result can be promoted to production approval.

## Completion Gate

The parent task completes only after a real Edition passes generation, human review, Publish Export, deployment, and public Feed acceptance, with all manual production decisions recorded. The current failed rerun is evidence of a blocked acceptance gate, not production success.
